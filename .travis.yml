# Sample .travis.yml for R projects

language: r
warnings_are_errors: true
sudo: false
cache: packages

notifications:
  email:
    on_success: true
    on_failure: true

after_success:
  - Rscript -e 'library(covr);coveralls()'
